subprojects {
    apply plugin: 'java'

    repositories {
        maven {
            url 'https://jbaruch.jfrog.io/jbaruch/build-faceoff/'
        }
    }

    dependencies {
        testCompile 'junit:junit:4.12'
    }
}

project(':war') {
    apply plugin: 'war'
}

